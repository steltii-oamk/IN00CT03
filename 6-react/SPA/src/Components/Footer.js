const Footer = (props) => {
    return (
        <footer>
            <div>
                <span>Copyright ©: {props.author}</span>
            </div>
        </footer>
    );
}

export default Footer;